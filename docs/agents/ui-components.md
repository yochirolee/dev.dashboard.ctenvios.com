# UI components (shadcn/ui)

- Use shadcn/ui components for common UI patterns.
- Radix UI primitives are available via shadcn components.

Guidance
- Prefer composing existing UI components before creating new bespoke ones.
- Keep component APIs consistent with existing usage in the codebase.
